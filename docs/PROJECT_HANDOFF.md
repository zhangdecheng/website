# FLOURISH CULTURE Website Project Handoff

Last updated: 2026-06-25

## Project background

This repository contains the static English landing page for FLOURISH CULTURE at `flourishculturekol.com`.

The current production direction is the approved Option 1, “Neon Culture Bridge”: a black, coral-red, high-impact global creator growth site. The site replaced the earlier white/green Chinese draft, which is retained only as a rollback/reference snapshot under `archive/2026-06-19-white-green-draft/`.

The site must remain pure static HTML/CSS/JavaScript:

- no API
- no database
- no cookies
- no analytics/tracking
- no server-side form handler
- no additional pages for this phase

The production server also hosts the separate `/review/` TikTok review application. Homepage releases must not modify Nginx configuration, certificates, `/review/` application files, `/review/` data, or service configuration.

## Source of truth

- Approved visual direction: `design-options/neon-culture-bridge.png`
- Section visual references: `design-options/sections/`
- Design brief: `docs/design-brief.md`
- Progress and implementation log: `PROJECT_PROGRESS.md`
- Visual QA result log: `design-qa.md`
- Manual review/editing copy: `review-editable.html`

`review-editable.html` is a local-only helper for manual browser editing. Confirmed changes should be copied back to `index.html`, `styles.css`, and `script.js`. It is intentionally excluded from generated release archives.

## Current production implementation

Primary source files:

- `index.html` — English single-page homepage structure
- `styles.css` — responsive visual system, section layout, focus/reduced-motion behavior
- `script.js` — mobile menu, smooth UI behavior, reveal logic, form handling
- `site-core.js` — shared mailto builders and validators
- `assets/` — self-hosted fonts, Remix Icon files, brand logos, creator/culture/service imagery

Fixed anchors:

- `#services`
- `#talent`
- `#about`
- `#contact`

Forms:

- Creator form opens a prefilled email to `irisa@flourishculture.com`
- Project inquiry form opens a prefilled email to `flourishculture@outlook.com`
- There is no fake server success state.

## Directory guide

- `assets/` — production assets served by the homepage
- `assets/brand-logos/` — approved brand endorsement logos only
- `assets/fonts/` — self-hosted font files
- `assets/icons/` — self-hosted Remix Icon CSS/font files
- `archive/` — preserved old white/green draft and fallback screenshots
- `canvas/` — Cowart/tldraw canvas state and page assets
- `design-options/` — approved mockup and section truth images
- `design-references/` — external reference captures used during design iteration
- `docs/` — design brief, handoff, asset inventory, and operational notes
- `feishu-annotations/` — exported Feishu whiteboard annotation references
- `qa/` — browser QA results, screenshots, comparison images, and visual QA notes
- `scripts/` — build and QA utility scripts
- `tests/` — Node test suite for content, forms, release constraints, and styling guardrails

Generated and ignored:

- `dist/` — generated by `npm run build`
- `release/flourishculturekol-homepage.zip` — generated from `dist/`
- `node_modules/` — restored by `npm install`

## Build and release commands

Local verification:

```bash
npm test
npm run build
cd dist && zip -qr ../release/flourishculturekol-homepage.zip .
```

Local preview:

```bash
npm run serve
```

Open `http://127.0.0.1:4173/`.

Production deployment, only after explicit approval:

```bash
bash deploy-cloud-assistant.sh /path/to/flourishculturekol-homepage.zip
```

The deploy script backs up the current web root, copies the static package into `/var/www/flourishculturekol.com`, restores web-readable file permissions, and validates Nginx syntax. It deliberately does not rewrite Nginx config and does not touch `/review/`.

## Production release notes

2026-06-25 production homepage release:

- Host: `150.5.135.196`
- Web root: `/var/www/flourishculturekol.com`
- Initial release backup: `/var/backups/flourishculturekol.com/20260625-114135`
- MIME hotfix backup: `/var/backups/flourishculturekol.com/20260625-135253`
- `/review/healthz` remained healthy after release.

Lessons from the release:

1. Do not judge a release by HTTP 200 alone; verify rendered page content and browser console.
2. Do not use `.mjs` browser module files unless Nginx is configured to serve `.mjs` as JavaScript. This project now uses `site-core.js`.
3. After copying static files from a temporary extraction directory, reset directory and file permissions so Nginx can read the web root.
4. Continue checking `/review/healthz` and `/review/` redirect behavior after homepage releases.

## Recovery

If the homepage needs rollback:

1. SSH to the production server.
2. Identify the desired backup under `/var/backups/flourishculturekol.com/`.
3. Restore the backup to `/var/www/flourishculturekol.com`.
4. Reapply web-readable permissions if needed.
5. Run `nginx -t`.
6. Verify homepage, key assets, and `/review/healthz`.

Do not patch production files piecemeal unless the root cause is understood and the change is minimal.

