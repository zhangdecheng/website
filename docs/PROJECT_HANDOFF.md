# FLOURISH CULTURE Website Project Handoff

Last updated: 2026-08-18

## Project background

This repository contains the English marketing site and its loopback Contact
service for FLOURISH CULTURE at `flourishculturekol.com`.

The current production direction is the approved Option 1, “Neon Culture Bridge”: a black, coral-red, high-impact global creator growth site. The site replaced the earlier white/green Chinese draft, which is retained only as a rollback/reference snapshot under `archive/2026-06-19-white-green-draft/`.

The v1.2.0 architecture is:

- static HTML/CSS/JavaScript marketing pages under `/var/www/flourishculturekol.com`;
- one Node.js >= 20 Contact service bound only to `127.0.0.1:3101`, using the stable
  `/opt/flourish-contact/runtime` symlink rather than system or Review Node;
- Nginx proxies only `/api/contact` to that service;
- Cloudflare Turnstile with mandatory server-side Siteverify;
- SMTP relay through the fixed `business@flourish-culture.com` identity;
- no database or analytics/tracking integration.

The production server also hosts the separate `/review/` TikTok review application.
The v1.2.0 release requires a minimal, reviewed Nginx change for canonical
redirects, Contact proxying and static-page security headers. It must preserve
certificate directives and all `/review/` locations, files, data and services.

## Source of truth

- Approved visual direction: `design-options/neon-culture-bridge.png`
- Section visual references: `design-options/sections/`
- Design brief: `docs/design-brief.md`
- Progress and implementation log: `PROJECT_PROGRESS.md`
- Visual QA result log: `design-qa.md`
- Manual review/editing copy: `review-editable.html`

`review-editable.html` is a local-only helper for manual browser editing. Confirmed changes should be copied back to `index.html`, `styles.css`, and `script.js`. It is intentionally excluded from generated release archives.

## Project version records

Current source version: `v1.2.0` local release candidate

| Version | Date | Status | Notes |
| --- | --- | --- | --- |
| `v1.2.0` | 2026-08-18 | Locally verified; production incomplete | Unified Contact API, privacy notice, risk controls, two approved AI images, deterministic release and strict production checks. |
| `v1.1.0` | 2026-06-26 | Historical production record | Static homepage release; exact commit currently serving production is not confirmed. |
| `v1.0.0` | 2026-06-26 | Historical record | Baseline project version record for documentation and handoff tracking. |

## Current source implementation

Primary source files:

- `index.html` — English single-page homepage structure
- `styles.css` — responsive visual system, section layout, focus/reduced-motion behavior
- `script.js` — mobile menu, smooth UI behavior and reveal logic
- `contact-form.js` — accessible unified form, Turnstile loading and same-origin submission
- `site-core.js` — form payload builders, stable endpoints and public messages
- `privacy.html` — generic privacy notice for the Contact flow
- `server/` — validation, abuse controls, Turnstile, immutable mail routing and HTTP service
- `ops/` — hardened systemd unit and minimal Nginx API template
- `assets/` — self-hosted fonts, Remix Icon files, brand logos, creator/culture/service imagery

Fixed anchors:

- `#services`
- `#talent`
- `#about`
- `#contact`

Contact routing:

- Brand: From `business@flourish-culture.com`, To `hannah@flourish-culture.com`
- Creator: From `business@flourish-culture.com`, To `irisa@flourishculture.com`
- visitor email is validated and used only as Reply-To
- success is shown only after the API returns `201` or duplicate-safe `202`

## Directory guide

- `assets/` — production assets served by the homepage
- `assets/brand-logos/` — approved brand endorsement logos only
- `assets/fonts/` — self-hosted font files
- `assets/icons/` — self-hosted Remix Icon CSS/font files
- `archive/` — preserved old white/green draft and fallback screenshots
- `canvas/` — Cowart/tldraw canvas state and page assets
- `design-options/` — approved mockup and section truth images
- `design-references/` — external reference captures used during design iteration
- `docs/` — design brief, handoff, asset inventory, and operational notes
- `feishu-annotations/` — exported Feishu whiteboard annotation references
- `qa/` — browser QA results, screenshots, comparison images, and visual QA notes
- `scripts/` — build and QA utility scripts
- `server/` — Contact API implementation
- `ops/` — production service/Nginx source templates
- `tests/` — Node test suite for content, forms, release constraints, and styling guardrails

Generated and ignored:

- `dist/` — generated by `npm run build`
- `release/flourishculturekol-homepage.zip` — generated from `dist/`
- `release/flourish-contact-service.tgz` — allow-listed Contact service package
- `release/SHA256SUMS` — transfer-only hashes, not committed
- `node_modules/` — restored by `npm install`

## Build and release commands

Local verification:

```bash
npm test
npm run build
npm run build:contact
node scripts/browser-qa.cjs
cd dist && zip -qr ../release/flourishculturekol-homepage.zip .
```

Local preview:

```bash
npm run serve
```

Open `http://127.0.0.1:4173/`.

Production deployment and rollback must follow `docs/PRODUCTION_RUNBOOK.md`.
Do not publish the static package before the protected service and Nginx proxy
pass loopback checks. Never move Turnstile Secret, SMTP authorization code or
the service security secret through Git, chat, screenshots, command arguments
or Cloud Assistant output.

## Production release notes

Public production baseline observed on 2026-08-18:

- DNS target: `150.5.135.196`
- Web root: `/var/www/flourishculturekol.com`
- `www.flourishculturekol.com` and `flourishculturekol.com` resolved to `150.5.135.196`.
- Both HTTPS hostnames returned the same historical homepage; apex did not yet redirect to www.
- Confirmed cloud account `2103632597`, region `cn-hongkong`, ECS
  `i-yeo9geadc0plsv0abgv0`, and Nginx source
  `/etc/nginx/conf.d/00-flourishculturekol.com.conf`.
- Confirmed Review production/staging use independent Node 22 processes on 8787/8788 and pass
  local TLS/SNI health checks. Contact will use the existing independent Node 24 path through its
  own stable symlink.
- Do not use `AI-OpenClaw-b6uN-000` / `118.196.85.61` for homepage releases unless DNS and server routing are intentionally changed. On 2026-06-26, that candidate host redirected the FLOURISH host to `/todolist/`.

Historical release backup records (current existence not yet re-verified):

- Initial release backup: `/var/backups/flourishculturekol.com/20260625-114135`
- MIME hotfix backup: `/var/backups/flourishculturekol.com/20260625-135253`
- v1.1.0 release backup: `/var/backups/flourishculturekol.com/20260626-000044`
- Minimal asset release backup: `/var/backups/flourishculturekol.com/20260626-113529`
- `/review/healthz` remained healthy after release.

2026-06-26 v1.1.0 homepage release:

- Source: GitHub `main`, commit `3e33cd6`, package version `1.1.0`.
- Release archive SHA256: `10a8f930776f54866e459455ed8ec15545ed9f7c6a50ff7358864fb80d63660e`.
- Post-release verification confirmed the public homepage HTML, CSS, `script.js`, and `site-core.js` hashes matched local `dist/`, new JPEG assets returned HTTP 200 with valid dimensions, `/review/healthz` remained healthy at review version `2.0.4`, and `/review/` still redirected to `/review/login`.

2026-06-26 minimal asset homepage release:

- Release archive SHA256: `96d36e6aef3319076daf105fbe6d519fc20d90f11abb772b84e3646d96fa8992`.
- The release build now copies only required static assets, rewrites production photo fallbacks to compressed WebP files, and keeps only required brand logos, fonts, and Remix Icon WOFF2.
- Transfer used a temporary GitHub artifact branch because no production SSH key was available locally; the branch was deleted after the server downloaded and verified the package.
- Post-release verification confirmed public HTML, CSS, `script.js`, and `site-core.js` hashes matched local `dist/`, key WebP assets returned HTTP 200, `/review/healthz` remained healthy at review version `2.0.4`, and Nginx config passed.

Lessons from the release:

1. Do not judge a release by HTTP 200 alone; verify rendered page content and browser console.
2. Do not use `.mjs` browser module files unless Nginx is configured to serve `.mjs` as JavaScript. This project now uses `site-core.js`.
3. After copying static files from a temporary extraction directory, reset directory and file permissions so Nginx can read the web root.
4. Continue checking `/review/healthz` and `/review/` redirect behavior after homepage releases.
5. Before publishing, confirm DNS and server behavior identify the correct production host; the historical homepage target is `150.5.135.196`, not every similarly named ECS instance.
6. Keep the release archive minimal: do not copy the entire `assets/` tree when the homepage only needs optimized WebP photos, brand logos, fonts, and icon fonts.

## Recovery

If the homepage needs rollback:

1. SSH to the production server.
2. Identify the desired backup under `/var/backups/flourishculturekol.com/`.
3. Restore the backup to `/var/www/flourishculturekol.com`.
4. Reapply web-readable permissions if needed.
5. Run `nginx -t`.
6. Verify homepage, key assets, and `/review/healthz`.

Do not patch production files piecemeal unless the root cause is understood and the change is minimal.
