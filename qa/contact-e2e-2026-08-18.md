# FLOURISH Contact E2E Acceptance — 2026-08-20

## 结论

状态：**部分完成（生产提交与 SMTP 接受已通过；收件箱读回未完成）**。

用户重新输入正确的 Cloudflare Turnstile Secret 后，ECS 只读核验确认 Secret 不再等于
公开 Site Key（Secret 长度 35、Site Key 长度 24）；Cloudflare Siteverify 对隐藏 Secret
返回 HTTP 200，并仅因诊断使用假 token 而给出预期的 `invalid-input-response`。随后在
生产站点分别执行新的 Brand 与 Creator 真实提交，两次 Managed Turnstile 均显示
`成功！`，页面均返回 `Thank you—your message has been received.`，ECS journal 分别记录
`role=brand` / `role=creator`、`outcome=accepted`。随后用户报告初次 Creator 邮件发送失败；
核对发现运行路由使用了错误地址 `irisa@flourishculture.com`（缺少连字符）。

已将 Creator 固定收件人更正为 `irisa@flourish-culture.com`，按测试先行完成两项回归
测试红—绿验证，并将新 Contact release 部署至 ECS。更正后使用唯一标识
`E2E-CREATOR-ROUTE-FIX-20260820T111525Z` 再提交一次，页面再次返回成功，ECS journal
记录新请求 `role=creator`、`outcome=accepted`。

当前运行 release `/opt/flourish-contact/releases/20260820T110838Z` 的邮件路由文件与
本地已审计文件 SHA-256 均为
`47d40d2c61664b9b3000bc665f0f57ac0339d4a3ed5599f8d9d1a4c77030b235`，确认代码将 Brand
固定路由给 Hannah、Creator 固定路由给 Irisa，并使用固定 From 与访客邮箱 Reply-To。
但 Hannah、Irisa 的收件箱邮件头、正文及回复草稿尚未实际读回；因此不能把 SMTP
`accepted` 表述为“最终进入收件箱”，端到端验收仍有最后一项未完成。

## 已确认的测试边界

- 受控访客邮箱：`zdc1219910501@icloud.com`（用户明确指定）。
- Brand 固定收件人：`hannah@flourish-culture.com`。
- Creator 固定收件人：`irisa@flourish-culture.com`。
- 固定发件人：`business@flourish-culture.com`。
- 生产站点：`https://www.flourishculturekol.com/#contact`。
- 真实 E2E 内容将在执行时生成唯一 UTC 标识：`E2E-BRAND-<UTC>` 与
  `E2E-CREATOR-<UTC>`；不得复用历史失败请求。

## 提交前基线（已读回）

2026-08-20T00:17:22Z 在 ECS `webhkhome` 只读确认：

- `nginx`、`flourish-contact`、Review production/staging 均为 `active`；Contact 为
  `enabled`，仅监听 `127.0.0.1:3101`。
- Contact health 返回 `{"ok":true,"configured":true,"version":"1.2.0"}`。
- Contact 当前 release 为 `/opt/flourish-contact/releases/20260819T202836Z`，运行时为
  `/opt/node-v24.17.0-linux-x64`。
- `/etc/flourish-contact.env` 仅核对为 `root:flourish-contact 0640`、362 bytes；未读取值。
- systemd 启动日志包含 `smtp_authentication_accepted`，未回显凭据。
- 完整生产验收脚本 SHA-256
  `dba3ae7de17beada857e07750e6d1e13eb715cdaf68e10d7366e7e28d7eb5ad8`
  再次全部通过；`/review/healthz` 为 `200`，`/review/` 为 `302` 到登录页。
- 历史真实请求 `471c2340-0dab-4269-bb4e-7124a7a1e5ee` 使用旧配置时返回
  `invalid-input-secret`，并在 SMTP 前停止；它不是新配置成功证据。

## Brand 验收

| 项目 | 当前状态 | 证据 |
| --- | --- | --- |
| 真实 Managed Turnstile 完成 | 已确认 | Safari 页面显示 Turnstile `成功！`，提交按钮从 disabled 变为 enabled |
| 页面返回成功（API `201`/`202`） | 已确认 | 唯一标识 `E2E-BRAND-20260820T094026Z`；页面显示 `Thank you—your message has been received.` |
| request ID / UTC 时间 | 已确认（成功请求） | `0daa90f6-0365-4675-9bf8-b72653f153a2` / `2026-08-20T09:48:27.157Z`；`outcome=accepted`；`durationMs=5769` |
| Hannah 收件箱实际到达 | 未确认 | SMTP 已接受，但尚未从 Hannah 收件箱读回邮件 |
| From / To / Subject 正确 | 实现已确认；邮件头未读回 | 运行文件哈希与已审计代码一致：From `business@flourish-culture.com`、To `hannah@flourish-culture.com`、Subject `[Flourish Website] New Brand Inquiry` |
| Reply-To 为受控访客邮箱 | 实现已确认；回复草稿未读回 | 运行代码将规范化前的访客邮箱写入 `replyTo`；尚未从实际邮件打开回复草稿 |
| Brand 字段齐全且无 Creator 字段 | 实现已确认；邮件正文未读回 | 运行代码的 Brand 字段清单仅包含 Name、Email、Company、Budget、Growth Objectives |

## Creator 验收

| 项目 | 当前状态 | 证据 |
| --- | --- | --- |
| 新的 Managed Turnstile 完成 | 已确认 | 更正路由后的 Safari 页面显示 Turnstile `成功！`，提交按钮从 disabled 变为 enabled |
| 页面返回成功（API `201`/`202`） | 已确认 | 唯一标识 `E2E-CREATOR-ROUTE-FIX-20260820T111525Z`；页面显示 `Thank you—your message has been received.` |
| request ID / UTC 时间 | 已确认（更正后成功请求） | `6129340b-43d5-4475-83c2-81362c178e8a` / `2026-08-20T11:28:05.017Z`；`outcome=accepted`；`durationMs=4921` |
| 旧错误路由请求 | 已确认（不可作为送达证据） | `7631f334-dbca-4c98-a657-5e878e319c40` 使用错误地址 `irisa@flourishculture.com`；虽被 SMTP 接受，但用户报告发送失败 |
| Irisa 收件箱实际到达 | 未确认 | 更正后 SMTP 已接受，但尚未从 Irisa 新地址收件箱读回邮件 |
| From / To / Subject 正确 | 实现已确认；邮件头未读回 | 运行文件哈希与已审计代码一致：From `business@flourish-culture.com`、To `irisa@flourish-culture.com`、Subject `[Flourish Website] New Creator Application` |
| Reply-To 为受控访客邮箱 | 实现已确认；回复草稿未读回 | 运行代码将规范化前的访客邮箱写入 `replyTo`；尚未从实际邮件打开回复草稿 |
| Creator 字段齐全且无 Brand 字段 | 实现已确认；邮件正文未读回 | 运行代码的 Creator 字段清单仅包含 Name、Email、Social Media Handles、Niche、Main Audience Demographics |

## 安全日志验收

已按失败 request ID、初次成功 request ID 与路由更正后的成功 request ID 读回结构化 journal 行；仅包含 request
ID、UTC 时间、耗时、role、outcome、reason、diagnostic（仅失败路径）、IP 哈希和邮箱
哈希。未出现姓名、原始邮箱、表单正文、Turnstile token、SMTP 授权码或 Turnstile
Secret。失败与成功路径的日志脱敏均为**已确认**。

## 后续硬门禁

1. 由 Hannah、Irisa 分别读回收件箱邮件，核对 From、To、Subject、正文与字段隔离。
2. 分别点击“回复”并仅打开草稿，确认 Reply-To 为 `zdc1219910501@icloud.com`；不得发送。
3. 仅在上述收件箱验收完成后，才将 Contact E2E 状态改为完成。
4. 推送 GitHub `main` 与删除 GitHub Pages 属于独立的外部写入/永久变更，仍需在执行时
   单独确认，不能由本次表单测试授权代替。

## 最终生产复核与验收入口

- 验收 URL：`https://www.flourishculturekol.com/`；Contact 锚点：
  `https://www.flourishculturekol.com/#contact`。
- 2026-08-20T12:00Z 后在 ECS 使用 `/opt/flourish-contact/runtime/bin/node` 对应的
  Node v24 PATH 执行受版本控制的 `check-https-cloud-assistant.sh`，Nginx、loopback
  health、规范跳转、首页/Privacy 精确哈希与安全头、Contact API、静态资源以及
  `/review/` 健康/登录跳转全部通过。
- Fresh local verification: `npm test` 86/86；`npm run test:release` 19/19。
- Rebuilt Contact artifact: 9,006 bytes；SHA-256
  `ddb674d7d65ea3273009f293c2de8e70135da1749650b9651b556277aa72aa3b`。
- Rebuilt 12-file transfer artifact: 1,022,160 bytes；SHA-256
  `c8f53f5fcb3cb051a8db78c6b808b9fb783a0309c17464fc704120808741c3cb`。
- 以上足以提交生产 URL 做页面与提交链路验收；不等同于 Hannah/Irisa 实际收件箱
  与 Reply-To 验收完成。
