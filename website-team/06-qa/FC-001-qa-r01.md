# FC-001｜质量验收报告 r01（只读核验，不发布）

| Field | Value |
| --- | --- |
| 项目/任务 | FLOURISH-WEBSITE / FC-001 |
| 作者 | 工程 QA（Ethan） |
| 版本 | r01｜2026-09-17 16:02 WITA（UTC+8） |
| 状态 | **候选本地/源码验收完成** — 未 push、未 merge、未部署 |
| 候选 | PR https://github.com/zhangdecheng/website/pull/2 ；分支 `cursor/fc-001-creators-page-6c84` |
| 候选 SHA | `bc5f63b5b76aa6ebab2c4e89c9f4b12766c66496` |
| Base expected / actual | `codex/flourish-site-refresh` @ `577753f52fe0ffd86293409edc5d9c805061e0e9`（merge-base 一致） |
| 环境 | 本地只读：`/workspace/website-team/website`；`gh` 已认证为 zhangdecheng；公网只读 curl（未提交表单、未部署） |
| 输入 | `04-content/FC-001-content-candidates-r01.md`；`02-product/FC-001-prd-r02.md` AC-06/07/08（及 AC-09 产物视角）；`05-engineering/FC-001-tech-eval-r01.md` |

---

## 1. 总结果

| 范围 | 结果 |
| --- | --- |
| **本地源码 + `npm run build` dist + 静态预览抽核** | **PASS**（AC-06/07/08 在候选包内满足；文案对齐 content r01；`/review/` 未改；邮件路由未改） |
| **生产公网 AC-06 / AC-07** | **BLOCKED / 未测通过**（未授权部署；现网仍为 SPA 软回退首页，见 tech-eval R1） |
| **全量 `npm test`** | **条件通过**：FC-001 相关 site/contact 用例全绿；3 个 `release.test.mjs` 静态 rollout 用例因本机缺 `/usr/bin/rsync` 失败（环境依赖，非本候选回归证据） |

**一句话结论：** 候选 SHA `bc5f63b` 可作为「待授权发布前」技术验收基线；**不得**将生产 AC-06/07 标 PASS，直至真实文件进公网并消除软回退。

---

## 2. 证据路径与命令摘要

| 证据 | 路径 / 命令 | 结果摘要 |
| --- | --- | --- |
| HEAD SHA | `git rev-parse HEAD` | `bc5f63b5b76aa6ebab2c4e89c9f4b12766c66496` |
| 相对 base 提交 | `git log origin/codex/flourish-site-refresh..HEAD` | `90d24aa` feat + `bc5f63b` browser QA |
| 改动面 | `git diff --stat origin/codex/flourish-site-refresh...HEAD` | 19 files；含 `creators/index.html`、`robots.txt`、`sitemap.xml`、`contact-form.js`、`index.html`、构建/门禁/测试 |
| `/review/` | 同 diff 文件名检索 | **无** review 文件改动 |
| 内容对齐 | 源码 `index.html` / `creators/index.html` vs content r01 | title/H1/hero/四条/5,000 any one platform/CTA 对齐；JSON-LD 已去掉 `primaryImageOfPage: null` |
| 预选逻辑 | `contact-form.js` `contactRoleFromLocation` + `tests/contact-form.test.mjs` | 读取 `?role=creator`（及 hash 内 query）；`CONTACT_ROLES = brand|creator` |
| 构建 | `npm run build` → `dist/` | 含 `creators/index.html`、`robots.txt`、`sitemap.xml`；`review*` 不在 dist |
| dist SHA | `sha256sum dist/index.html dist/creators/index.html` | home `41cdf6d1…34ec` ≠ creators `f13d05c4…9a53` |
| 静态预览 | `python3 -m http.server 4173 --directory dist` + curl | `/creators/` 正文 ≠ 首页；`/robots.txt` `text/plain`；`/sitemap.xml` 含绝对 URL `https://www.flourishculturekol.com/creators` |
| 公网（只读） | curl `www.flourishculturekol.com/{,/creators/,/robots.txt}` | **三者同首页 SHA 前缀 `c96f7bc5…`；robots `content-type: text/html`** → 软回退仍在 |
| 测试 | `npm test`（安装 `zip` 后） | 约 96 pass / 3 fail（rsync ENOENT）；`node --test tests/site.test.mjs tests/contact-form.test.mjs` → **37/37 pass**（含 creators 独立页、robots/sitemap、role 预选） |

---

## 3. 逐项 AC

### 3.1 内容对照（支撑 T 验收，非替代 Emma 业务 AC）

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 首页 title / description / 首屏一句 / Explore + Apply 入口 | **PASS** | `index.html` 与 content r01 §1 一致 |
| `/creators` title / H1 / lead / 四条 / Who / How / CTA | **PASS** | `creators/index.html` 与 content r01 §2 一致 |
| 粉丝门槛「any one platform」5,000+ | **PASS** | creators 正文 + meta |
| 服务三支柱用词 | **PASS** | brand matching / operations support / growth coaching |
| 禁区（教程承诺、无来源业绩数字、sole Contact Us） | **PASS（抽核）** | creators 无 sole「Contact Us」；无 PDF 业绩数字上标题/H1 |

> AC-01–05 / AC-13 业务结论仍归 Emma；本报告不替代业务验收。

### 3.2 技术 AC（Ethan）

| ID | 预期（PRD / tech-eval） | 本地候选 | 生产公网 | 判定 |
| --- | --- | --- | --- | --- |
| **AC-06** | 独立 URL；200；正文≠首页；独立 title/H1；自指 canonical；无意外 noindex | `creators/index.html` 独立；title/H1/canonical=`…/creators`；无 noindex；dist SHA ≠ 首页 | `GET /creators/` **200 但正文=首页**（软回退） | **本地 PASS** / **生产 BLOCKED** |
| **AC-07** | 真实 robots 文本；sitemap 含新页绝对 URL | `robots.txt` 非 HTML；Allow `/`、Disallow `/review/`、Sitemap 绝对地址；`sitemap.xml` 含 `/`、`/creators`、`/privacy.html`；已进 `build-release.mjs` 白名单与 artifacts 必有列表 | `/robots.txt` 仍为 **HTML 首页** | **本地 PASS** / **生产 BLOCKED** |
| **AC-08** | 创作者页 CTA → 既有表单且 Creator 预选；不改邮件路由 | CTA → `/?role=creator#contact`；`contactRoleFromLocation` + 单测 PASS；`server/` 无 diff；未改收件人配置 | 未做生产表单提交抽测（本轮明确不做） | **源码/单测 PASS**；生产邮件送达 **未测** |
| **AC-09** | 源码 / 构建包 / 公网一致（须另授权发布） | 源码 allow-list ⊇ 新页+robots+sitemap；`npm run build` dist 一致；zip/artifacts 全链路未作为本轮发布执行 | 公网仍为旧首页包 | **源码↔dist PASS**；**公网一致 BLOCKED（未部署）** |

### 3.3 tech-eval 风险回写

| 风险 | 本轮状态 |
| --- | --- |
| R1 软回退未进包仍显示首页 | **生产仍成立** → AC-06/07 生产 BLOCKED（符合「未授权部署前标未测/BLOCKED」） |
| R2 跨页未预选 | **候选已补** `role` query + 单测 |
| R3 clean URL / 不改 Nginx | 实现为 `creators/index.html` + 根绝对资源路径；**生产是否命中目录 index 须部署后实读** |
| R6 误动 review / 邮件路由 | **未发生**（diff 无 review；无 server 改动） |

---

## 4. 测试明细

| 套件 | 结果 | 说明 |
| --- | --- | --- |
| `tests/site.test.mjs` + `tests/contact-form.test.mjs` | **PASS（37/37）** | 含「creators page is an independent…」「robots.txt and sitemap.xml…」「contactRoleFromLocation reads query role=creator…」 |
| `npm test` 全量（装 zip 后） | **96 pass / 3 fail** | 失败三项均在 `tests/release.test.mjs` 静态 rollout：本机 `/usr/bin/rsync` ENOENT（fault injection / rollback 健康与 manifest）。**不作为 FC-001 页面内容 FAIL**；记为环境未测完整发布模拟 |
| 首次全量（装 zip 前） | 另有 zip ENOENT | 已装 `zip` 后该批消失；报告以装 zip 后为准 |

---

## 5. 未测项 / BLOCKED 项

1. **生产 AC-06 / AC-07**：未授权部署 → **BLOCKED**（现网探针已证明仍软回退）。  
2. **生产 AC-09 三一致**：未部署 → **BLOCKED**。  
3. **生产 AC-08 端到端点击 + Creator 邮件送达**：未授权、本轮不做发信 → **未测**。  
4. **无扩展名 `/creators`（无尾斜杠）在真实 Nginx 上的目录 index 行为**：仅本地静态服验证了 `/creators/`；生产须部署后实读（tech-eval U2）。  
5. **GSC / 收录 / 效果 AC-10–12**：非本轮 Ethan QA 范围 → **未测**。  
6. **release 静态 rollout 故障注入/回滚用例**：缺系统 `rsync` → **本环境未完整跑通**（与候选功能正交）。

---

## 6. 明确未做

- 未 `git push`、未 merge PR、未执行 `deploy-cloud-assistant.sh`、未改 Nginx、未提交联系表单、未改邮件路由。

---

## 7. 下一步

1. **@产品经理（Emma）**：按 AC-01–05 / AC-13 对同一候选做业务验收（文案与主张）。  
2. **@项目主管（Alex）**：汇总「本地技术 PASS + 生产 AC-06/07 BLOCKED」；向用户申请**单独发布授权**前勿合入/部署。  
3. **授权发布后 Ethan**：公网复测 AC-06/07/09（SHA/MIME/非软回退）；可选 AC-08 生产点击（邮件抽测另授权）。  
4. 可选：CI/验收机补齐 `rsync`（及已补的 `zip`）以便 release 模拟用例全绿。

---

## 8. 群消息草稿

@产品经理（Emma） @项目主管（Alex） FC-001 候选 PR#2（`cursor/fc-001-creators-page-6c84` @ `bc5f63b`）Ethan 只读质量验收：**本地源码/dist/静态预览 AC-06/07/08 PASS**（creators≠首页、robots 为文本、sitemap 含绝对 `/creators`、`?role=creator` 预选有单测）；**生产 AC-06/07 因未部署仍软回退 → BLOCKED**。报告：`06-qa/FC-001-qa-r01.md`。未 push/merge/部署；请 Emma 做业务 AC，Alex 等用户发布授权后再排公网复测。

---

*报告结束。证据以本机命令与文件为准；结论不含未授权生产通过声明。*
