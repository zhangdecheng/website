# FC-001｜质量验收报告 r02（PR#2 已合并；生产部署 BLOCKED）

| Field | Value |
| --- | --- |
| 项目/任务 | FLOURISH-WEBSITE / FC-001（仅 PR#2；**不含** Hero Scheme A / PR#1） |
| 作者 | 工程 & 质量 Lead（Ethan） |
| 版本 | r02｜2026-09-17 16:12 WITA（UTC+8） |
| 状态 | **Git merge PASS** + **本地构建/门禁 PASS**；**生产部署 BLOCKED**（本机无 Volcengine/Aliyun Cloud Assistant CLI 与凭据）；公网 AC-06/07/09 **仍 FAIL（软回退）** |
| 候选 / 合并 | PR https://github.com/zhangdecheng/website/pull/2 → **MERGED** |
| Head（合并前核验） | `bc5f63b5b76aa6ebab2c4e89c9f4b12766c66496`（与授权 SHA 完全一致） |
| Merge commit / base tip | `5b179257a67da28fa67e42bb9dad309be3d42a1a` on `codex/flourish-site-refresh` |
| 环境 | `/workspace/website-team/website`；`gh` 认证为 zhangdecheng；公网只读 curl；**未改 Nginx EXPECTED_NGINX_SHA / 生产 Nginx** |
| 范围声明 | 未 merge、未部署 `preview/hero-scheme-a` / PR#1 |

---

## 1. 总结果

| 步骤 | 结果 |
| --- | --- |
| 1. Merge PR#2 @ `bc5f63b` | **PASS** — `gh pr ready 2` + `gh pr merge 2 --merge`；state=MERGED；merge commit `5b179257…` |
| 2. 构建产物 zip | **PASS** — `npm run build` + `npm run build:artifacts`；zip SHA = deploy 脚本 `EXPECTED_ARCHIVE_SHA` |
| 3. 本地 dist/check 对 EXPECTED_* | **PASS** — 15/15 关键文件（含 creators/robots/sitemap）与 `deploy-cloud-assistant.sh` 常量一致 |
| 4. 生产 Cloud Assistant 部署 | **BLOCKED** — 本 box **无** `ve` / `volcengine` / `aliyun` CLI；无 `~/.volc*` 凭据；无法对 `i-yeo9geadc0plsv0abgv0`（webhkhome）Invoke/RunCommand。按 runbook，部署脚本须在 **服务器本机** 对 `/var/www/flourishculturekol.com` 执行，本机无法发起 Cloud Assistant 任务。 |
| 5. 公网 AC-06 / 07 / 09 | **FAIL（预部署软回退仍在）** — `/creators/`、`/robots.txt`、`/sitemap.xml` 正文 SHA = 现网首页 `c96f7bc5…`（= `EXPECTED_OLD_HOME_SHA`） |

**一句话结论：** FC-001 代码已安全合入 `codex/flourish-site-refresh`，发布包与门禁哈希已就绪且与 `deploy-cloud-assistant.sh` 对齐；**因缺少 Volcengine Cloud Assistant CLI/凭据，生产静态文件未上线**。公网仍为旧包软回退，**不得**宣称生产 AC-06/07/09 PASS。

---

## 2. Merge 证据

| 项 | 值 |
| --- | --- |
| PR | #2 `feat(fc-001): creators page, homepage light copy, robots/sitemap` |
| 合并前 head SHA | `bc5f63b5b76aa6ebab2c4e89c9f4b12766c66496` |
| mergeable / mergeStateStatus | MERGEABLE / CLEAN（合并前） |
| 操作 | `gh auth setup-git` → `gh pr ready 2` → `gh pr merge 2 --merge --delete-branch=false` |
| state / mergedAt | MERGED / `2026-09-17T08:11:01Z`（= 16:11 WITA） |
| mergedBy | zhangdecheng |
| mergeCommit | `5b179257a67da28fa67e42bb9dad309be3d42a1a` |
| `origin/codex/flourish-site-refresh` tip | `5b179257a67da28fa67e42bb9dad309be3d42a1a` |
| 本地 checkout | fast-forward `577753f` → `5b17925`；`git rev-parse HEAD` = merge tip |
| Hero / PR#1 | **未触碰**（`preview/hero-scheme-a` 仍停在本地 `bfdf6d6`，未 merge） |

---

## 3. 构建与门禁哈希

命令：`npm ci`；`npm run build`；`npm run build:artifacts`（已安装 `rsync`/`zip`）。

| 产物 | SHA-256 |
| --- | --- |
| `release/flourishculturekol-homepage.zip` | `b3368a182d8c9db727c76389bdecb800bc14372a8a8703f9845cd9e0928be0ae` |
| `check-https-cloud-assistant.sh` | `8486adfef2f2b3575fc123eea39c2196b066548bf7c43ab15237ed740971f884` |
| `deploy-cloud-assistant.sh`（脚本自身） | `42f740036a29c27cd29ba2fe804276d80ae0700aacf81649e0c8d9584c6030b6` |
| `dist/index.html` | `41cdf6d1fdc2f514d348677aa3203dbe2cb496c3040859e14f82c7da07be34ec` |
| `dist/creators/index.html` | `f13d05c489d902da7866707c6e8a22a4bdcf765651c84402449011bdde049a53` |
| `dist/robots.txt` | `2e988a2cae0368746dfb504ba79ebc5662abaf1410a82d4a9fa2e109f92a688e` |
| `dist/sitemap.xml` | `bb0eadfa45fd76630d37bbe6068eb209df5df89cda3acc98d1fc5cfdaf7876db` |
| `dist/contact-form.js` | `52001c78a05015aef867073b5e5127e2d3548436cee478267e679eb4b4380ff8` |
| `dist/privacy.html` | `a36c172a358b2325b752e8b87e3f08c548ec035cf0de3f24bcdc0d28c2b404c2` |

与 `deploy-cloud-assistant.sh` 中 `EXPECTED_ARCHIVE_SHA` / `EXPECTED_CHECK_SCRIPT_SHA` / `EXPECTED_NEW_*` / `EXPECTED_CREATORS_SHA` / `EXPECTED_ROBOTS_SHA` / `EXPECTED_SITEMAP_SHA` 等 **15 项全部 MATCH（ALL_MATCH）**。

未修改 `EXPECTED_NGINX_SHA`（仍为 `22efa58a328b5855999638133acc13a9472fa27132b1cba675479adbe2904d3e`）。

本地测试（本轮）：`node --test tests/site.test.mjs tests/contact-form.test.mjs tests/release.test.mjs` → **58/58 pass**（含 creators 独立页、robots/sitemap、release 门禁）。

---

## 4. 部署结果：**BLOCKED**

### 缺口（精确）

| 缺口 | 实测 |
| --- | --- |
| Volcengine CLI (`ve`) | `type ve` → not found；`command -v ve` 空 |
| `volcengine` / `aliyun` | 均不存在 |
| 凭据目录 | 无 `~/.volc*` / `~/.ve` / `~/.config/volc*`；环境变量无 `VOLC*`/`ALIYUN*` AK |
| 既有 invoke 自动化 | `07-release/` 空；repo docs/qa 仅描述人工在有 CLI 的终端执行 `ve ecs …` / 服务器上 `sudo bash deploy-cloud-assistant.sh …` |
| 目标实例 | 文档：`i-yeo9geadc0plsv0abgv0` / hostname `webhkhome` / 区域 `cn-hongkong` — **本机无法 Describe/RunCommand** |

### 若具备 CLI 时应执行的路径（未执行，仅记录）

按 `docs/PRODUCTION_RUNBOOK.md`「静态站点发布」：

```bash
# 在 ECS 上（经 Cloud Assistant 投递或已登录主机）：
sudo bash deploy-cloud-assistant.sh \
  /absolute/path/to/flourishculturekol-homepage.zip \
  /absolute/path/to/check-https-cloud-assistant.sh
# WEB_ROOT=/var/www/flourishculturekol.com
# 不得改 Nginx；脚本会 assert EXPECTED_NGINX_SHA
```

本轮 **未** 上传 zip、**未** 调用 Cloud Assistant、**未** 声称生产发布成功。

现网首页 SHA `c96f7bc5f291e292473b4603bc55587710464e5272867d2e69888530ca4a39e1` = 脚本 `EXPECTED_OLD_HOME_SHA`，说明 **pre-deploy 旧包状态仍成立**（部署门禁前置条件仍满足，只差投递能力）。

---

## 5. 公网探针证据（https://www.flourishculturekol.com）

采样时间：2026-09-17 ~16:11–16:12 WITA。命令：`curl -sI` + body `sha256`。

| URL | HTTP | Content-Type | Body SHA-256 | 相对首页 |
| --- | --- | --- | --- | --- |
| `/` | 200 | `text/html` | `c96f7bc5f291e292473b4603bc55587710464e5272867d2e69888530ca4a39e1` | — |
| `/creators/` | 200 | `text/html` | **同首页** `c96f7bc5…` | **软回退** |
| `/creators` | 200 | `text/html` | **同首页** `c96f7bc5…` | **软回退** |
| `/robots.txt` | 200 | **`text/html`**（非 text/plain） | **同首页** `c96f7bc5…` | **软回退** |
| `/sitemap.xml` | 200 | **`text/html`**（非 XML） | **同首页** `c96f7bc5…` | **软回退**；无 `/creators` URL |
| `/privacy.html` | 200 | `text/html` | `a36c172a358b2325b752e8b87e3f08c548ec035cf0de3f24bcdc0d28c2b404c2` | ≠ 首页；与本地 dist/EXPECTED_PRIVACY 一致（既有页） |

`/creators/` 与 `/` 的 `<title>` 均为 `FLOURISH CULTURE | Global Influencer Marketing`（非独立 creators title）。`content-length: 28247` 对 `/`、`/creators/`、`/robots.txt`、`/sitemap.xml` 相同。

`curl -sI` 片段（节选）：`server: nginx`；上述路径均为 `HTTP/2 200` + `content-type: text/html` + 相同 etag `"12ce3580-6e57"`。

---

## 6. 逐项 AC（生产视角）

| ID | 预期 | 本地（合并 tip / dist） | 生产公网 | 判定 |
| --- | --- | --- | --- | --- |
| **AC-06** | `/creators` 独立文档；正文≠首页；独立 title/H1 | dist creators SHA `f13d05c4…` ≠ home `41cdf6d1…`；独立 title/H1 | `/creators/` 正文=首页；title 相同 | **本地 PASS** / **生产 FAIL（软回退；部署 BLOCKED）** |
| **AC-07** | robots=`text/plain`；sitemap=XML 且含绝对 `/creators` | dist robots/sitemap SHA 匹配 EXPECTED_*；sitemap 含 `https://www.flourishculturekol.com/creators` | robots/sitemap 均为 HTML 首页 | **本地 PASS** / **生产 FAIL** |
| **AC-09** | 源码/构建/公网一致 | 源码→dist→zip 与 EXPECTED_* 全一致 | 公网 creators/robots/sitemap **未** 反映 zip；home 仍为 OLD | **源码↔产物 PASS** / **公网一致 FAIL（未部署）** |

Nginx：本轮 **未** 修改配置，也 **未** 解锁/改写 `EXPECTED_NGINX_SHA`。

---

## 7. 解阻清单（给 Alex / 运维）

1. 在具备 Volcengine 凭据的终端安装/配置 `ve` CLI（账号文档：`2103632597` / `cn-hongkong`）。
2. 确认 `ve ecs DescribeCloudAssistantStatus --InstanceIds.1 i-yeo9geadc0plsv0abgv0` 为 Running。
3. 将本仓库 tip `5b17925` 的 `release/flourishculturekol-homepage.zip`（SHA `b3368a18…be0ae`）与 `check-https-cloud-assistant.sh`（SHA `8486adfe…f884`）投递到实例，再执行 `deploy-cloud-assistant.sh`（**勿改 Nginx**）。
4. 部署后复测：`/creators/` body SHA = `f13d05c4…9a53`；`/robots.txt` Content-Type text/plain；`/sitemap.xml` 含绝对 creators URL。

---

## 8. 群发草稿（中文，Ethan → @Alex @Emma）

见本报告末「附录」；可直接复制。

---

## 附录｜中文群消息草稿

@Alex @Emma  
【FC-001 发布状态｜Ethan｜2026-09-17 16:12 WITA】  
1）PR#2 已合并进 `codex/flourish-site-refresh`，merge commit `5b179257…`（head 已核验 `bc5f63b`）。**未动 Hero/PR#1。**  
2）本地构建通过：`flourishculturekol-homepage.zip` SHA `b3368a18…be0ae`，与 `deploy-cloud-assistant.sh` 全部 EXPECTED_*（含 creators/robots/sitemap）一致。  
3）**生产部署 BLOCKED**：本执行环境无 Volcengine/`ve` CLI 与凭据，无法对 `i-yeo9geadc0plsv0abgv0`（webhkhome）走 Cloud Assistant。请 Alex 在有 CLI 的终端按 runbook 投递 zip + 跑 `deploy-cloud-assistant.sh`（**不要改 Nginx**）。  
4）公网复测仍软回退：`/creators/`、`/robots.txt`、`/sitemap.xml` 正文 SHA = 现网首页 `c96f7bc5…`，AC-06/07/09 **生产未过**。完整证据：`06-qa/FC-001-qa-r02.md`。部署完成后我再复测标 PASS。
