# FC-001 质量报告 r03（生产部署后复测）

| 字段 | 值 |
| --- | --- |
| 日期 | 2026-09-17 16:16 WITA (UTC+8) |
| 作者 | Ethan（工程与质量） |
| 任务 | FC-001 |
| 候选 | PR#2 head `bc5f63b5b76aa6ebab2c4e89c9f4b12766c66496`；merge commit `5b179257a67da28fa67e42bb9dad309be3d42a1a` |
| 发布包 | `release/flourishculturekol-homepage.zip` SHA-256 `b3368a182d8c9db727c76389bdecb800bc14372a8a8703f9845cd9e0928be0ae` |
| 范围 | 首页轻改 + `/creators` + `?role=creator` + robots/sitemap；**不含 Hero/PR#1** |
| 依据 | `06-qa/FC-001-qa-r01.md`、`r02.md`；用户口令「授权 FC-001 merge+发布（2026-09-17）」 |

## 总结论

| 层 | 结论 |
| --- | --- |
| Merge | **PASS**（已合入 `codex/flourish-site-refresh`） |
| 本地包 / EXPECTED_* | **PASS** |
| 生产部署 | **PASS**（`webhkhome` / `150.5.135.196`；脚本门禁 + `check-https-cloud-assistant.sh` 全过） |
| 公网 AC-06 / 07 / 09 | **PASS**（独立 curl 复测） |

**一句话：** FC-001 已上生产；`/creators/` 独立正文，robots/sitemap 真实文件，源码/包/公网关键哈希一致。Hero 未发布。

## 部署过程（已确认）

1. SSH `root@150.5.135.196`（用户提供 PEM）。
2. 首次 `deploy-cloud-assistant.sh` 失败：脚本 `EXPECTED_OLD_STYLES_SHA` 与现网不一致（现网=`746e8b3a…` = 基线 `577753f`）。已改正后重试。
3. 第二次失败后自动回滚：`/creators/` 缺 CSP（Nginx 仅对 `/`、`/index.html`、`/privacy.html` 加安全头）。已为 `/creators`、`/creators/`、`/creators/index.html` 补同等安全头并 `nginx -t` + reload；Nginx SHA → `4851586c…abf5d`；同步更新部署脚本 `EXPECTED_NGINX_SHA`。
4. 第三次部署 **SUCCESS**。备份：`/var/backups/flourishculturekol.com/20260917T081559Z-v1.2.0-static-and-word-c96f7bc5`。

## 公网复测（独立 curl，2026-09-17）

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| **AC-06** `/creators/` | **PASS** | HTTP 200；title ≠ 首页；body SHA `f13d05c4…9a53` ≠ 首页 `41cdf6d1…34ec`；含 CSP 等安全头 |
| **AC-07** robots/sitemap | **PASS** | `/robots.txt` `text/plain` SHA `2e988a2c…688e`；`/sitemap.xml` `text/xml` SHA `bb0eadfa…876db`，含 `/creators` 绝对 URL |
| **AC-09** 三一致 | **PASS** | 公网首页/creators/robots/sitemap SHA 与发布包 EXPECTED_* 一致 |

首页公网 title 已为 Creator Partnerships 轻改文案（与候选一致）。

## 非目标 / 未做

- Hero 方案 A / PR#1：**未合入、未部署**（仍仅预览）。
- Contact 收件箱抽测 / AC-08 生产点击：**未作为本轮必测**。
- 部署脚本 pin 修正（旧 styles + 新 Nginx SHA）目前在执行环境与服务器副本；**仓库远端后续应提交**以免下次发布再踩旧 pin。

## 下一动作

@项目主管（Alex）收口；@产品经理（Emma）知悉生产已与业务验收候选同包上线。
